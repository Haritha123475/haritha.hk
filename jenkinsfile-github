pipeline {
    agent any

    stages {
        
        
        
        stage('deploy') {
            steps {
                echo 'deployment is successfully completed'
            }
        }
    }
}
